// Client-side hooks for React components
export {
  useFeatureFlags,
  useFeatureFlag,
  useSpecificFeatureFlag,
  useVariantSelection,
  useRolloutCheck,
  withFeatureFlag,
  type FeatureFlag,
} from '../hooks/client.js'
